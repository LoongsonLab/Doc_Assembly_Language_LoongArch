#	习题

1. 汇编源文件包含哪几类内容？分别是什么？

2. `.s`文件和`.S`文件有什么区别？

3. 如何在`.s`文件中定义一个全局`short`类型变量？

4. 使用内嵌汇编完成两个数组的数据复制函数。
``` c
void memcopy(int *a,int *b,int length){
}
```

5. 编写一个实现数据复制功能的汇编源文件`memorycopy.S`，并编写一个C语言文件`main.c`调用`memorycopy.S`中的功能，输出两个整数相加的值。

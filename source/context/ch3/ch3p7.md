#	习题

1. LoongArch中的寻址方式有哪几种？

2. 在LA64架构下，执行下列指令后，寄存器r5的值是多少？
``` asm
li.d 	$r4, 0x7fffffff
add.w 	$r5, $r4, $r4
```

3. 针对如下C语言循环语句，编写其对应的汇编指令。
``` c
for(int i = 0;i < 100;i++)	a[i] += 2;
```

4. 编写用于读取当前处理器是否支持硬件对齐访存的汇编指令。

5. 使用指令`crc.w.d.w`编写一个更加高效的CRC程序。

6. 位操作指令仅在LA64架构上支持，那么在LA32架构上同样的功能应如何实现？请举例说明。

7. 如何保证多线程之间对同一共享区域内数据的正确读写？

8. LoongArch中的跳转指令有几类？分别是什么？

9. LoongArch中的预取指令是什么？请写出预取指定内存地址中128位数据的汇编指令。

10. 简述LoongArch中原子访存指令AMO和LL-SC在使用上的区别。
